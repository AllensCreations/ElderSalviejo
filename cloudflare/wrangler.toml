name = "elder-salviejo-email-trigger"
main = "email-worker.js"
compatibility_date = "2024-09-01"

# Environment Variables (Configure these in Cloudflare Dashboard or with `npx wrangler secret put`)
[vars]
# Optional fallback website URL
SITE_URL = "https://eldersalviejo.vercel.app"

# Optional: Cloudflare Cron Triggers (e.g. check every minute if not using Email Routing)
# [triggers]
# crons = ["* * * * *"]
